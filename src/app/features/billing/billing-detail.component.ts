import { Component, inject, signal } from '@angular/core';
import { CommonModule } from '@angular/common';
import { ActivatedRoute, RouterLink } from '@angular/router';
import { FormsModule } from '@angular/forms';
import { BillingApiService } from '../../core/api/billing-api.service';
import { BillingPayment, Invoice, Quotation } from '../../shared/models/billing.models';

@Component({ selector: 'bm-billing-detail', standalone: true, imports: [CommonModule, RouterLink, FormsModule], template: `
<div class="max-w-5xl mx-auto space-y-6">@if (error()) { <div class="bm-card p-4 text-rose-700">{{ error() }}</div> } @if (record()) { <div class="flex flex-wrap justify-between gap-4"><div><a [routerLink]="['/app/billing', type()]" class="text-sm text-emerald-700">← Back</a><h1 class="text-3xl font-bold text-slate-900 mt-2">{{ number(record()!) }}</h1><p class="text-sm text-slate-500">{{ record()!.title }} · {{ record()!.status }}</p></div><div class="flex gap-2"><a [routerLink]="['/app/billing', type(), record()!.id, 'edit']" class="bm-btn bm-btn-secondary">Edit</a>@if (type() === 'quotations' && record()!.status !== 'converted') { <button class="bm-btn bm-btn-primary" (click)="convert()">Convert to Invoice</button> }</div></div><div class="grid grid-cols-1 sm:grid-cols-3 gap-4"><div class="bm-card p-4"><div class="text-xs text-slate-500">Subtotal</div><div class="text-xl font-bold">AED {{ money(record()!.subtotal) }}</div></div><div class="bm-card p-4"><div class="text-xs text-slate-500">Tax</div><div class="text-xl font-bold">AED {{ money(record()!.tax_amount) }}</div></div><div class="bm-card p-4 bg-emerald-50"><div class="text-xs text-emerald-700">Total</div><div class="text-xl font-bold text-emerald-800">AED {{ money(record()!.total_amount) }}</div></div></div><div class="bm-card overflow-x-auto"><h2 class="p-5 font-semibold border-b">Line Items</h2><table class="w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="p-4">Particulars</th><th class="p-4">Quantity</th><th class="p-4">Unit Price</th><th class="p-4 text-right">Total</th></tr></thead><tbody class="divide-y">@for (line of record()!.lines || []; track line.id) { <tr><td class="p-4">{{ line.particulars }}</td><td class="p-4">{{ line.quantity }}</td><td class="p-4">AED {{ money(line.unit_price) }}</td><td class="p-4 text-right">AED {{ money(line.line_total) }}</td></tr> }</tbody></table></div><div class="bm-card p-5 space-y-4"><div class="flex flex-wrap justify-between gap-3"><div><h2 class="font-semibold">Payment Lines</h2><p class="text-xs text-slate-500 mt-1">Posted lines generate Accounts receipts or vouchers.</p></div><button class="bm-btn bm-btn-primary text-xs" (click)="showPaymentForm.set(!showPaymentForm())">{{ showPaymentForm() ? 'Close' : 'Add Payment Line' }}</button></div>@if (showPaymentForm()) { <form (ngSubmit)="addPayment()" class="grid grid-cols-1 md:grid-cols-3 gap-3 p-4 rounded-xl bg-slate-50"><select class="bm-input" [(ngModel)]="payment.direction" name="direction"><option value="inward">Inward</option><option value="outward">Outward</option></select><input class="bm-input" [(ngModel)]="payment.particulars" name="particulars" placeholder="Particulars" required><input class="bm-input" type="number" min="0.01" step="0.01" [(ngModel)]="payment.amount" name="amount" placeholder="Amount" required><input class="bm-input" type="date" [(ngModel)]="payment.due_date" name="due_date"><select class="bm-input" [(ngModel)]="payment.payment_mode" name="payment_mode"><option value="cash">Cash</option><option value="cheque">Cheque</option><option value="bank_transfer">Bank Transfer</option></select><button class="bm-btn bm-btn-primary">Save Payment Line</button></form> }<div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="p-3">Direction</th><th class="p-3">Particulars</th><th class="p-3">Mode</th><th class="p-3">Amount</th><th class="p-3">Status</th><th class="p-3">Action</th></tr></thead><tbody class="divide-y">@for (line of record()!.payments || []; track line.id) { <tr><td class="p-3 capitalize">{{ line.direction }}</td><td class="p-3">{{ line.particulars }}</td><td class="p-3 capitalize">{{ line.payment_mode.replace('_',' ') }}</td><td class="p-3">AED {{ money(line.amount) }}</td><td class="p-3 capitalize">{{ line.status }}</td><td class="p-3"><button class="text-emerald-700 font-semibold text-xs" [disabled]="line.status === 'paid'" (click)="postPayment(line)">Mark Paid</button></td></tr> } @empty { <tr><td colspan="6" class="p-4 text-center text-slate-500">No payment lines.</td></tr> }</tbody></table></div></div> } @else if (!error()) { <div class="bm-card p-8 text-center text-slate-500">Loading...</div> }</div>` })
export class BillingDetailComponent {
  private api = inject(BillingApiService); private route = inject(ActivatedRoute);
  type = signal<'quotations' | 'invoices'>('quotations'); record = signal<Quotation | Invoice | null>(null); error = signal<string | null>(null); showPaymentForm = signal(false); payment: { direction: 'inward' | 'outward'; particulars: string; amount: number; due_date: string; payment_mode: string } = { direction: 'inward', particulars: '', amount: 0, due_date: '', payment_mode: 'cash' };
  constructor() { this.route.data.subscribe(data => this.type.set(data['type'] || 'quotations')); this.route.paramMap.subscribe(params => { const id = Number(params.get('id')); if (id) this.load(id); }); }
  load(id: number): void { const failure = (err: { message?: string }) => this.error.set(err.message || 'Unable to load document.'); if (this.type() === 'quotations') this.api.quotation(id).subscribe({ next: res => this.record.set(res.data), error: failure }); else this.api.invoice(id).subscribe({ next: res => this.record.set(res.data), error: failure }); }
  number(row: Quotation | Invoice): string { return 'quotation_no' in row ? row.quotation_no : row.invoice_no; }
  money(value: number | string | undefined): string { return Number(value || 0).toLocaleString('en-AE', { minimumFractionDigits: 2, maximumFractionDigits: 2 }); }
  convert(): void { const row = this.record(); if (!row || !('quotation_no' in row)) return; this.api.convertQuotation(row.id).subscribe({ next: () => this.load(row.id), error: err => this.error.set(err.message || 'Unable to convert quotation.') }); }
  addPayment(): void { const row = this.record(); if (!row || this.payment.amount <= 0 || !this.payment.particulars.trim()) return; const success = () => { this.showPaymentForm.set(false); this.payment = { direction: 'inward', particulars: '', amount: 0, due_date: '', payment_mode: 'cash' }; this.load(row.id); }; const failure = (err: { message?: string }) => this.error.set(err.message || 'Unable to add payment line.'); if ('quotation_no' in row) this.api.addQuotationPayment(row.id, this.payment).subscribe({ next: success, error: failure }); else this.api.addInvoicePayment(row.id, this.payment).subscribe({ next: success, error: failure }); }
  postPayment(line: BillingPayment): void { const row = this.record(); if (!row || line.status === 'paid') return; const success = () => this.load(row.id); const failure = (err: { message?: string }) => this.error.set(err.message || 'Unable to post payment.'); if ('quotation_no' in row) this.api.updateQuotationPayment(row.id, line.id, 'paid').subscribe({ next: success, error: failure }); else this.api.updateInvoicePayment(row.id, line.id, 'paid').subscribe({ next: success, error: failure }); }
}
